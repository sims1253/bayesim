# Result Schema Version

Version identifier for result column contract. Increment this when
result column names or types change.

## Usage

``` r
RESULT_SCHEMA_VERSION
```

## Format

An object of class `integer` of length 1.
