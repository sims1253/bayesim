# List committed delta-store shard descriptors through a checkpoint.

List committed delta-store shard descriptors through a checkpoint.

## Usage

``` r
list_delta_shard_refs(directory, prefix, checkpoint_id)
```
