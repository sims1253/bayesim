# Resolve the response name from a fit_spec (LHS of fit_spec\$formula). Returns NA_character\_ if it cannot be resolved.

Resolve the response name from a fit_spec (LHS of fit_spec\$formula).
Returns NA_character\_ if it cannot be resolved.

## Usage

``` r
.fit_spec_response_name(fit_spec)
```
