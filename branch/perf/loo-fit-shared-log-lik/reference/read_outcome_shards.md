# Validate and optionally materialize accumulated canonical outcome shards.

Validate and optionally materialize accumulated canonical outcome
shards.

## Usage

``` r
read_outcome_shards(result_path, refs, load_outcomes = TRUE)
```
