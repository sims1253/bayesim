# Capture Fitter Specification

Creates a hashable representation of a fitter's configuration.

## Usage

``` r
capture_fitter_spec(fitter)
```

## Arguments

- fitter:

  An S7 Fitter object or NULL.

## Value

A list or NA if NULL.
