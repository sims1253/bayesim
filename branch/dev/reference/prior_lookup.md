# Lookup default priors for a model family

Lookup default priors for a model family

## Usage

``` r
prior_lookup(family)
```

## Arguments

- family:

  A string specifying the model family (e.g., "gaussian", "student_t")

## Value

A list of brms prior objects for the specified family
