# Check if object is a bayesim_simulation_result

Check if object is a bayesim_simulation_result

## Usage

``` r
is_bayesim_simulation_result(x)
```

## Arguments

- x:

  Object to check

## Value

`TRUE` if `x` inherits from `"bayesim_simulation_result"`, `FALSE`
otherwise
