# Validate a resolved retention specification

Validate a resolved retention specification

## Usage

``` r
validate_resolved_retention_spec(value)
```

## Arguments

- value:

  A resolved retention spec (named list with context entries)

## Value

TRUE if valid, or an error message string
