# Construct the operational policy for a simulation run.

Construct the operational policy for a simulation run.

## Usage

``` r
new_run_policy(config, progress = TRUE, workers = NULL, verbose = TRUE)
```
