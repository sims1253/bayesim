# Should the run stop early based on the adaptive-stopping policy?

Should the run stop early based on the adaptive-stopping policy?

## Usage

``` r
bayesim_adaptive_check(task_results_so_far, task_grid, stop_on, config)
```

## Value

`TRUE` when every condition cell meets the precision target.
