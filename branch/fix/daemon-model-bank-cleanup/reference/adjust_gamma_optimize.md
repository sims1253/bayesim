# Exact gamma for a single sample (L = 1) via dynamic programming.

Exact gamma for a single sample (L = 1) via dynamic programming.

## Usage

``` r
adjust_gamma_optimize(N, K, conf_level = 0.95)
```
