# Check if object is a bayesim_fit_result

Check if object is a bayesim_fit_result

## Usage

``` r
is_bayesim_fit_result(x)
```

## Arguments

- x:

  Object to check

## Value

`TRUE` if `x` inherits from `"bayesim_fit_result"`, `FALSE` otherwise
