# Check if object is a bayesim_task_result

Check if object is a bayesim_task_result

## Usage

``` r
is_bayesim_task_result(x)
```

## Arguments

- x:

  Object to check

## Value

`TRUE` if `x` inherits from `"bayesim_task_result"`, `FALSE` otherwise
