% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/worker.R
\name{relative_eff_from_chains}
\alias{relative_eff_from_chains}
\title{Per-observation relative efficiency from the fit's chain structure}
\usage{
relative_eff_from_chains(fitter, fit_result, ll)
}
\description{
Mirrors brms' internal \code{r_eff_log_lik()} by deriving the chain id per draw
from \code{posterior::as_draws_df(fit)$.chain} and passing it to
\code{loo::relative_eff(exp(ll), chain_id = ...)}. \code{ll} here is S x N (draws x
observations, matching brms' log_lik orientation): \code{relative_eff} wants the
draws dimension as rows so chain_id (length S) matches \code{nrow(ll)}. Returns
one r_eff per observation (length N). Returns NULL when chain information is
unavailable (e.g. MockFitter or a fitter whose fit lacks a \code{.chain}
variable), in which case the caller falls back to \code{r_eff = NULL}.
}
\keyword{internal}
