% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results.R
\name{new_fit_result}
\alias{new_fit_result}
\title{Create a new bayesim_fit_result object}
\usage{
new_fit_result(
  success = TRUE,
  fit = NULL,
  draws = NULL,
  diagnostics = list(),
  timing = list(total = 0, warmup = 0, sample = 0),
  warnings = character(),
  error = NULL,
  data_bundle = NULL
)
}
\arguments{
\item{success}{Logical scalar indicating if the fit succeeded}

\item{fit}{The backend fit object (may be NULL if removed by retention policy)}

\item{draws}{Matrix of posterior draws (S x P), with column names for parameters}

\item{diagnostics}{Named list of diagnostic values (scalars or named numeric vectors)}

\item{timing}{List containing total, warmup, and sample timing in seconds}

\item{warnings}{Character vector of warning messages captured during fitting}

\item{error}{A condition object if the fit failed, NULL otherwise}

\item{data_bundle}{Optional list containing the training/test data used for fitting}
}
\value{
A validated \code{bayesim_fit_result} object
}
\description{
Constructs a result object from a Bayesian model fitting operation.
}
\details{
The bayesim_fit_result class encapsulates all outputs from a Bayesian model
fitting operation, including the posterior draws, diagnostics, timing information,
and any warnings or errors encountered.

Validation rules:
\itemize{
\item If \code{success} is FALSE, \code{error} must be non-NULL
\item If \code{success} is TRUE, \code{error} must be NULL
\item \code{timing$total} must be non-negative
\item If \code{draws} is not NULL, it must be a matrix with column names
}
}
\examples{
\dontrun{
# Successful fit
draws <- matrix(rnorm(1000), ncol = 2, nrow = 500)
colnames(draws) <- c("alpha", "beta")
result <- new_fit_result(
  success = TRUE,
  draws = draws,
  diagnostics = list(rhat = c(alpha = 1.01, beta = 1.00)),
  timing = list(total = 10.5, warmup = 5.0, sample = 5.5)
)

# Failed fit
result <- new_fit_result(
  success = FALSE,
  error = simpleError("Convergence failed"),
  diagnostics = list(),
  timing = list(total = 2.0, warmup = 2.0, sample = 0)
)
}
}
\seealso{
\link{Fitter}, \code{\link[=fit_model]{fit_model()}}
}
