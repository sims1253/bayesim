% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators.R
\name{forward_sim_generator}
\alias{forward_sim_generator}
\title{Construct a fitter-agnostic forward-simulation (IFS) data generator}
\usage{
forward_sim_generator(
  fitter,
  fit_spec,
  pilot_bundle,
  predictor_generator,
  response = NULL,
  n_draws = NULL
)
}
\arguments{
\item{fitter}{An S7 \link{Fitter} object supporting \code{\link[=predict_fit]{predict_fit()}}.}

\item{fit_spec}{A list (single-row fit_grid entry) with at least \code{formula}.}

\item{pilot_bundle}{A \code{data_bundle} list used for the one-time
preconditioning fit.}

\item{predictor_generator}{Function \verb{(data_spec, task_ctx) -> data.frame}
producing predictor covariates. Must consume the ambient RNG state.}

\item{response}{Name of the response column. Defaults to
\code{pilot_bundle$response}, falling back to the LHS of \code{fit_spec$formula},
then to \code{"y"}.}

\item{n_draws}{Optional integer override for the number of draws to store.}
}
\value{
A generator function \verb{(data_spec, task_ctx) -> data_bundle}.
}
\description{
\code{forward_sim_generator()} is the fitter-agnostic analogue of
\code{\link[=ifs_generator]{ifs_generator()}}. It fits the model ONCE on \code{pilot_bundle}, draws theta from
the posterior (via \code{\link[=extract_draws]{extract_draws()}}), and forward-simulates \code{y} via the
fitter's \code{\link[=predict_fit]{predict_fit()}} (posterior-predictive) at the chosen draw's
parameters. Works with any Fitter that supports \code{\link[=predict_fit]{predict_fit()}}
(LinearRegressionFitter, BrmsFitter, ...).
}
\details{
Unlike \code{\link[=prior_draws_generator]{prior_draws_generator()}} (which targets the prior), this generator
concentrates the truth draw in a region of high posterior mass, which is
practical for diffuse or improper priors. Note this is not by itself a
valid SBC configuration: valid SBC requires the fitting prior to match the
theta-generating distribution (here the pilot posterior), so a diffuse or
unmatched fitting prior yields systematically non-uniform (cap-shaped)
ranks; see the caveat in \code{\link[=ifs_generator]{ifs_generator()}}.

Because forward simulation here relies on \code{\link[=predict_fit]{predict_fit()}}, the response is
drawn exactly as the fitter implements its posterior-predictive sampling,
which respects the fitter's response distribution and link function. Each
task uses a distinct draw, deterministically indexed by \code{task_ctx$rep_idx}.
}
\section{Limitations}{

The \code{true_params} reported by this generator are the \code{\link[=extract_draws]{extract_draws()}}
columns for the selected draw; for \link{LinearRegressionFitter} these are
\code{Intercept}, \verb{<coef>}, \code{sigma}. The response is forward-simulated via the
fitter's \code{\link[=predict_fit]{predict_fit()}} applied to the predictor design (a single Gaussian
draw for Gaussian fitters). Fitters without \code{\link[=predict_fit]{predict_fit()}} support are not
supported (e.g. raw \link{CmdStanFitter}, which has no newdata semantics).
}

\seealso{
\code{\link[=ifs_generator]{ifs_generator()}}, \code{\link[=prior_draws_generator]{prior_draws_generator()}}, \link{Fitter},
\link{LinearRegressionFitter}
}
