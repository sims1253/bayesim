% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contracts.R
\name{validate_fitter_interface}
\alias{validate_fitter_interface}
\title{Validate Fitter Interface}
\usage{
validate_fitter_interface(fitter)
}
\arguments{
\item{fitter}{An S7 object to validate as a Fitter.}
}
\value{
The input \code{fitter}, invisibly, if validation passes.
}
\description{
Validates that a fitter object conforms to the Fitter S7 class interface.
The fitter must be an S7 object that inherits from the Fitter class and
implements all required methods.
}
\details{
The fitter must satisfy the following requirements:
\itemize{
\item Must be an S7 object (checked via S7::S7_inherits())
\item Must inherit from the "Fitter" class
\item Must implement the following methods:
\itemize{
\item \code{fit(data_bundle, fit_spec, seed, task_ctx)}
\item \code{extract_draws(fit_result, variables = NULL)}
\item \code{predict(fit_result, newdata = NULL, seed = NULL)}
\item \code{log_lik(fit_result, newdata = NULL)}
\item \code{loo(fit_result)}
\item \code{diagnostics(fit_result)}
}
}
}
\section{Errors}{

Throws a \code{bayesim_contract_error} condition if validation fails.
}

\examples{
# Validate the mock fitter
mock_fitter <- MockFitter()
validate_fitter_interface(mock_fitter)
}
\seealso{
\link{Fitter}, \link{MockFitter}
}
