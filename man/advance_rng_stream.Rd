% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rng.R
\name{advance_rng_stream}
\alias{advance_rng_stream}
\title{Advance RNG stream}
\usage{
advance_rng_stream(rng_stream, n = 1L)
}
\arguments{
\item{rng_stream}{Integer vector RNG state}

\item{n}{Number of steps to advance (default: 1)}
}
\value{
Advanced RNG state (integer vector)
}
\description{
Advances the RNG state by n steps and returns the new state.
}
\details{
This function temporarily sets \code{.Random.seed} in the calling environment
to advance the stream, then returns the new state. It does NOT permanently
modify the global \code{.Random.seed}. The caller is responsible for applying
the returned state if needed (e.g., via \code{set_task_rng()}).
}
\examples{
\dontrun{
streams <- create_task_rng_streams(42, 10)
advanced <- advance_rng_stream(streams[[1]], n = 5)
}
}
\keyword{internal}
