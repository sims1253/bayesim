% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics-built-in.R
\name{resolve_draw_columns}
\alias{resolve_draw_columns}
\title{Resolve requested cleaned var names to actual draws-matrix columns}
\usage{
resolve_draw_columns(vars, draws_colnames)
}
\arguments{
\item{vars}{Character vector of cleaned names (e.g. \code{c("x","Intercept", "sigma")}).}

\item{draws_colnames}{Character vector of available draws-matrix column
names.}
}
\value{
A \strong{named} character vector: names are the cleaned \code{vars} (use
these for output field naming), values are the actual draws column to
read. Empty input returns \code{character(0)} (names preserved).
}
\description{
Maps each name in \code{vars} to itself, then to \verb{b_<name>}, in the set of
\code{draws_colnames}, mirroring the both-directions lookup \code{.extract_truth()}
already performs. This fixes the F2 mismatch where generators strip the
\code{b_} prefix (so \code{vars_of_interest} holds cleaned names like \code{c("x", "Intercept")}) but brms draws matrices keep it (\code{c("b_x","b_Intercept", "sigma")}).
}
\details{
Errors with a \code{bayesim_config_error} (same condition class as
\code{.extract_truth()}) when a requested var is genuinely absent. A silent NA
would corrupt SBC ranks and credible intervals without diagnostics.
}
\keyword{internal}
