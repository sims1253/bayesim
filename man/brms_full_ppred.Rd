% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators.R
\name{brms_full_ppred}
\alias{brms_full_ppred}
\title{Full forward sampling of a brms model's responses for a SINGLE posterior draw}
\usage{
brms_full_ppred(fit, newdata = NULL, draw = 1L)
}
\arguments{
\item{fit}{A brmsfit with posterior draws.}

\item{newdata}{Optional data.frame of predictors. If \code{NULL}, uses \code{fit$data}.}

\item{draw}{Single integer draw index to simulate.}
}
\value{
A data.frame (a copy of \code{newdata}) with the simulated response
column(s) filled in.
}
\description{
Simulates the response(s) of a brmsfit at a single posterior draw, respecting
dependency order among multiple responses. Ported from the SBC package and
the 0.x bayesim codebase; bayeshear/SBC/future dependencies removed.
}
\details{
\code{ifs_generator()} only ever needs one draw per task (the deterministic
\code{rep_idx}-indexed draw), so this function takes a single \code{draw} index and
returns a single data.frame. This eliminates the prior index mismatch where
results were stored at \code{pp_data[[draw_value]]} but read at \code{simulated[[1]]}.
}
\keyword{internal}
