% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-fitter.R
\name{update_prefit}
\alias{update_prefit}
\title{Update a prefit with new data (model bank path)}
\usage{
update_prefit(
  prefit,
  fitter,
  data_bundle,
  seed,
  formula,
  family,
  prior,
  stanvars
)
}
\description{
Runs \code{stats::update()} with \code{recompile = FALSE}. BEFORE updating, verifies
that the task data + formula + family produce the same Stan \emph{data structure}
as the prefit (via \code{brms::make_standata()}). brms does NOT warn when
\code{recompile = FALSE} is passed against structurally incompatible data — it
silently reuses the compiled binary against a wrong model frame — so this
explicit structural comparison is the only reliable guard. Raises a fatal
\code{\link[=bayesim_internal_error]{bayesim_internal_error()}} on any mismatch (e.g. different predictor count,
new factor levels, missing variables).
}
\details{
Note: we compare the Stan \emph{data structure} (\code{make_standata} field names and
the design-matrix column count \code{K}), NOT \code{make_stancode}, because the latter
embeds data-derived default-prior values (e.g. the intercept's \code{student_t}
location = \code{mean(y)}) that vary across datasets without affecting whether the
compiled binary is reusable.
}
\keyword{internal}
