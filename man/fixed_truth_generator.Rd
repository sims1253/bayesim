% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators.R
\name{fixed_truth_generator}
\alias{fixed_truth_generator}
\title{Construct a fixed-truth data generator}
\usage{
fixed_truth_generator(truth, draw_data)
}
\arguments{
\item{truth}{Named numeric vector; the data-generating parameters, used as
\code{true_params} and \code{vars_of_interest} for every generated bundle.}

\item{draw_data}{Function with signature \verb{(data_spec, task_ctx)} returning a
list with at least \code{train} (data.frame). May optionally return \code{response},
\code{test}, \code{references}, \code{meta}.}
}
\value{
A generator function \verb{(data_spec, seed, task_ctx) -> data_bundle}.
}
\description{
Returns a generator closure that delegates to a user-supplied \code{draw_data}
function, pinning \code{data_bundle$true_params} to the supplied \code{truth} for every
task. Use this when the data-generating truth is known and constant (e.g. a
fixed effect size sweep).
}
\details{
The \code{draw_data} function must have signature \verb{(data_spec, task_ctx) -> list(train, test, response, ...)} WITHOUT \code{true_params}/\code{vars_of_interest} —
those are injected by the factory from \code{truth}. It must consume the ambient
RNG state (do not call \code{set.seed}/\code{withr::with_seed} inside).
}
\examples{
\dontrun{
gen <- fixed_truth_generator(
  truth = c(beta = 1, sigma = 1),
  draw_data = function(data_spec, task_ctx) {
    n <- data_spec$n \%||\% 20L
    x <- stats::rnorm(n)
    y <- x + stats::rnorm(n)
    list(train = data.frame(y = y, x = x), response = "y")
  }
)
}
}
