% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-bank.R
\name{model-bank}
\alias{model-bank}
\title{Model Bank for BrmsFitter}
\description{
Internal infrastructure that compiles each distinct brms model
spec once (via \code{brms::brm(chains = 0)}) and reuses the resulting prefit
across simulation tasks via \code{stats::update(recompile = FALSE)}. This
eliminates catastrophic recompilation in studies that fit the same model
to thousands of datasets.

The bank is transported to mirai daemons via a session option
(\code{bayesim.model_bank}) rather than as an S7 fitter property, so it does not
corrupt the config fingerprint (\code{capture_fitter_spec()} hashes S7
properties only). See \code{\link[=set_model_bank]{set_model_bank()}} / \code{\link[=get_model_bank]{get_model_bank()}}.
}
\keyword{internal}
