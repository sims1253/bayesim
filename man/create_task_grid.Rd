% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task-grid.R
\name{create_task_grid}
\alias{create_task_grid}
\title{Create Task Grid from Configuration}
\usage{
create_task_grid(config)
}
\arguments{
\item{config}{A SimulationConfig S7 object.}
}
\value{
A tibble with one row per task, containing columns:
\itemize{
\item \code{task_id}: Character identifier in format "dXXX_fXXX_rXXXXX"
\item \code{data_idx}: Integer index into the data_grid (1-based)
\item \code{fit_idx}: Integer index into the fit_grid (1-based)
\item \code{rep_idx}: Integer replicate number (1 to n_replicates)
\item \code{rng_seed}: List column containing precomputed RNG stream for each task
\item \code{status}: Character status, initialized to "pending"
}
}
\description{
Generates the complete, deterministic task table for a simulation.
Tasks are ordered lexicographically by task_id for reproducibility.
}
\examples{
\dontrun{
config <- simulation_config(
  data_grid = data.frame(n = c(100, 500)),
  fit_grid = data.frame(model = c("baseline", "full")),
  data_generator = my_data_gen,
  n_replicates = 10L,
  seed = 42L
)

task_grid <- create_task_grid(config)
# task_grid has 2 * 2 * 10 = 40 rows
}
}
\keyword{internal}
