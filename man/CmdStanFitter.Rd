% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmdstan-fitter.R
\name{CmdStanFitter}
\alias{CmdStanFitter}
\title{CmdStan Fitter (user-supplied Stan programs)}
\usage{
CmdStanFitter(
  stan_file = NULL,
  stan_code = NULL,
  stan_data,
  log_lik = NULL,
  epred = NULL,
  chains = 4L,
  iter_warmup = 1000L,
  iter_sampling = 1000L,
  adapt_delta = NULL,
  max_treedepth = NULL,
  parallel_chains = 1L,
  init = NULL,
  ...
)
}
\arguments{
\item{stan_file}{Path to a \code{.stan} file, or NULL if \code{stan_code} is supplied.}

\item{stan_code}{Character string of Stan code (used when \code{stan_file} is
NULL). Either \code{stan_file} or \code{stan_code} is required.}

\item{stan_data}{Function \verb{function(data_bundle, fit_spec) -> list} mapping a
bayesim data bundle + fit spec to the Stan \code{data} list. Required.}

\item{log_lik}{Name of the generated-quantities log-lik vector in the Stan
program, or NULL if the program has no log_lik GQ. When NULL,
\code{supports_log_lik}/\code{supports_loo} are FALSE and elpd metrics degrade.}

\item{epred}{Optional name of an epred/mu GQ matrix or vector. When supplied,
\code{supports_predictions} is TRUE and \code{predict_epred} returns the in-sample GQ
matrix (S x N).}

\item{chains}{Integer number of MCMC chains.}

\item{iter_warmup}{Integer warmup iterations per chain.}

\item{iter_sampling}{Integer sampling iterations per chain.}

\item{adapt_delta}{Numeric adapt_delta control parameter, or NULL for Stan
default.}

\item{max_treedepth}{Integer max_treedepth control parameter, or NULL.}

\item{parallel_chains}{Integer number of chains to run in parallel.}

\item{init}{Passed through to \verb{$sample()} (NULL, "random", "0", or a list).}

\item{...}{Additional named arguments passed through to cmdstanr's \verb{$sample()}.}
}
\value{
An S7 \code{CmdStanFitter} object.
}
\description{
Run user-supplied Stan programs via cmdstanr without brms. Compilation is
cached by cmdstanr (by file hash); each daemon compiles-or-cache-hits on
first use, so there is no model-bank integration (D2).

The Stan program may declare generated-quantities blocks for \code{log_lik} (a
vector of pointwise log-likelihoods) and optionally \code{epred} (the expectation
\code{mu}). Declare their names via the \code{log_lik} / \code{epred} arguments.

\strong{Newdata prediction is out of scope} (raw Stan has no newdata semantics):
\code{supports_predictions} is FALSE unless \code{epred} is given, in which case
\code{predict_epred} returns the in-sample GQ matrix and \code{predict_fit} is
unsupported. Test-set metrics require brms or a custom fitter.
}
\examples{
\dontrun{
fitter <- CmdStanFitter(
  stan_file = "model.stan",
  stan_data = function(bundle, spec) list(N = nrow(bundle$train), y = bundle$train$y),
  log_lik = "log_lik"
)
}
}
\seealso{
\link{Fitter}, \link{BrmsFitter}, \link{LinearRegressionFitter}
}
