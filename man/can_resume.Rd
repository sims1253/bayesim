% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resume.R
\name{can_resume}
\alias{can_resume}
\title{Check if Resumable Run Exists}
\usage{
can_resume(result_path)
}
\arguments{
\item{result_path}{Character; path to results directory containing checkpoints.}
}
\value{
TRUE if a valid run can be resumed, FALSE otherwise.
}
\description{
Checks whether a valid checkpoint exists that can be resumed.
A run can be resumed if:
\itemize{
\item Both run_manifest.json and latest.json exist
\item latest.json points to a valid checkpoint_id
\item The referenced checkpoint can be read and validated
}
}
\details{
This is a cheap existence/validity probe: the checkpoint is validated
(checksums, ledger, shard integrity) with \code{load_outcomes = FALSE}, so the
full outcome history is never deserialized here. Callers that need the
outcomes use \code{\link[=load_for_resume]{load_for_resume()}} instead.
}
\keyword{internal}
