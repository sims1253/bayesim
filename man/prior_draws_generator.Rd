% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators.R
\name{prior_draws_generator}
\alias{prior_draws_generator}
\title{Construct a fitter-agnostic prior-draws data generator}
\usage{
prior_draws_generator(
  fitter,
  fit_spec,
  pilot_bundle,
  predictor_generator,
  response = NULL,
  n_draws = NULL
)
}
\arguments{
\item{fitter}{An S7 \link{Fitter} object (e.g. \link{LinearRegressionFitter}).}

\item{fit_spec}{A list (single-row fit_grid entry) carrying at least
\code{formula} (a base R formula). For \link{LinearRegressionFitter} a formula like
\code{y ~ x} is expected.}

\item{pilot_bundle}{A \code{data_bundle} list (\code{train}, \code{response}, etc.) used for
the one-time preconditioning fit. The caller is responsible for providing a
representative pilot dataset. Must contain a \code{train} data.frame whose
column names match the design implied by \code{fit_spec$formula}.}

\item{predictor_generator}{Function \verb{(data_spec, task_ctx) -> data.frame}
producing the design matrix of predictors (everything except the response).
Must consume the ambient RNG state.}

\item{response}{Name of the response column. Defaults to
\code{pilot_bundle$response}, falling back to the LHS of \code{fit_spec$formula},
then to \code{"y"}.}

\item{n_draws}{Optional integer override for the number of draws to store; if
\code{NULL} (default), uses the number of draws returned by \code{\link[=extract_draws]{extract_draws()}}.}
}
\value{
A generator function \verb{(data_spec, task_ctx) -> data_bundle}.
}
\description{
\code{prior_draws_generator()} is the fitter-agnostic analogue of
\code{\link[=prior_predictive_generator]{prior_predictive_generator()}}. It works through the S7 \link{Fitter} interface
rather than brms-specific functions, so it can be used with
\link{LinearRegressionFitter}, \link{BrmsFitter}, \link{CmdStanFitter}, or any custom
Fitter.
}
\details{
The factory fits the model ONCE on \code{pilot_bundle} (provided by the caller),
extracts parameter draws via \code{\link[=extract_draws]{extract_draws()}}, and stores them. The returned
closure, on each call, picks a draw deterministically indexed by
\code{task_ctx$rep_idx} (wrapped modulo the number of stored draws), uses it as
\code{true_params}, and forward-simulates \code{y} from the supplied predictors.

Forward simulation: the response is drawn from a Gaussian with mean equal to
the linear predictor \verb{X theta} (using the coefficient columns of the draw)
and standard deviation equal to the \code{sigma} column of the draw (if present,
else 1). This is the natural data-generating process for Gaussian linear
models — the common case for \link{LinearRegressionFitter}.
}
\section{Limitations (non-brms)}{

A true model prior is directly accessible only for brms fits
(\code{\link[brms:prior_draws]{brms::prior_draws()}}). For other fitters there is no generic
"prior_draws" S7 method, so this factory falls back to the draws stored on
the pilot fit. For \link{LinearRegressionFitter} those are NIG-prior-conditioned
posterior draws (the prior is weak by default, \code{prior_precision = 1e-6}),
which makes this an \emph{approximate} prior-predictive path concentrated on the
pilot's posterior region. Brms users who need full prior-predictive coverage
should prefer the brms-specific \code{\link[=prior_predictive_generator]{prior_predictive_generator()}}.
}

\seealso{
\code{\link[=prior_predictive_generator]{prior_predictive_generator()}}, \code{\link[=forward_sim_generator]{forward_sim_generator()}},
\link{Fitter}, \link{LinearRegressionFitter}
}
