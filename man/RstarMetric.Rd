% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics-extended.R
\name{RstarMetric}
\alias{RstarMetric}
\alias{rstar_metric}
\title{R* Convergence Metric}
\usage{
RstarMetric(
  name = character(0),
  needs = character(0),
  required = FALSE,
  summary_type = "mean",
  uncertainty = FALSE,
  method = "rf"
)

rstar_metric(name = "rstar", uncertainty = FALSE, method = "rf")
}
\arguments{
\item{name}{Character string naming the metric. Defaults to "rstar".}

\item{uncertainty}{Logical; see \code{\link[posterior:rstar]{posterior::rstar()}}. Defaults to \code{FALSE}.}

\item{method}{Character; classifier passed to \code{\link[posterior:rstar]{posterior::rstar()}}.}
}
\value{
A \code{RstarMetric} object.

A \code{RstarMetric} object.
}
\description{
Computes the R* MCMC convergence diagnostic (Lambert & Vehtari
2022) via \code{\link[posterior:rstar]{posterior::rstar()}}. R* measures whether a classifier can
identify the chain that generated a draw better than chance; values near 1
indicate convergence. Requires per-chain posterior draws, which are
extracted from the underlying fit object (\code{fit_result$fit}):
\itemize{
\item \strong{BrmsFitter}: \code{fit_result$fit} is a \code{brmsfit}; \code{posterior::as_draws_df()}
on it carries \code{.chain}.
\item \strong{CmdStanFitter}: \code{fit_result$fit$fit$draws()} returns a \code{draws_df} with
\code{.chain}.
}

Fitters whose \code{fit_result$fit} carries no chain info (e.g.
\code{LinearRegressionFitter}, which has only a flat S x P draws matrix) return
\code{NA_real_} with a warning, since R* is undefined without multiple chains.

\code{posterior::rstar()} additionally requires the \code{caret} package (and a
backend such as \code{ranger} for the default random-forest classifier); on any
error (e.g. missing dependencies, too few chains) the metric degrades
gracefully to \code{NA_real_}.

Constructor for RstarMetric.
}
\examples{
rstar_metric()
rstar_metric(uncertainty = FALSE)
}
\keyword{internal}
