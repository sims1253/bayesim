% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics-extended.R
\name{RankMetric}
\alias{RankMetric}
\alias{rank_metric}
\title{SBC Rank Metric}
\usage{
RankMetric(
  name = character(0),
  needs = character(0),
  required = FALSE,
  summary_type = "mean",
  thin = "auto"
)

rank_metric(name = "rank", thin = "auto")
}
\arguments{
\item{name}{Character string naming the metric. Defaults to "rank".}

\item{thin}{Thinning policy: \code{"auto"} (default), \code{FALSE}, or an integer stride.}
}
\value{
A \code{RankMetric} object.

A \code{RankMetric} object.
}
\description{
Computes Simulation-Based Calibration ranks: for each parameter
in \code{vars_of_interest}, counts how many (possibly thinned) posterior draws
are below the data-generating \code{true_params} value. Under correct
calibration the ranks are uniformly distributed on \verb{0..n_ranks-1}.

Autocorrelation in the posterior draws biases SBC rank uniformity (Talts
et al. 2018, §4.1). By default (\code{thin = "auto"}) the draws are thinned
toward the minimum bulk-ESS across the ranked variables before ranking:
this keeps ~ESS equally spaced draws, restoring near-independent samples
so the rank distribution is comparable to the standard SBC uniformity
test. \code{n_ranks} (posterior sample size after thinning + 1 possible ranks)
is reported per variable and is required by the SBC diagnostics
(\code{plot_rank_ecdf}).

Constructor for RankMetric.
}
\examples{
rank_metric()
rank_metric(thin = FALSE)
}
\keyword{internal}
