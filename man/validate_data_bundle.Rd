% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contracts.R
\name{validate_data_bundle}
\alias{validate_data_bundle}
\title{Validate a Data Bundle}
\usage{
validate_data_bundle(data_bundle)
}
\arguments{
\item{data_bundle}{A list containing data and metadata for a simulation task.}
}
\value{
The input \code{data_bundle}, invisibly, if validation passes.
}
\description{
Validates that a data_bundle conforms to the required structure for use
in simulation tasks. The data_bundle is the output of a data_generator
function and contains all data-related objects needed for model fitting
and metric computation.
}
\details{
The data_bundle must have the following structure:
\itemize{
\item \code{train}: A data.frame with at least 1 row (required)
\item \code{test}: NULL or a data.frame (optional)
\item \code{response}: A scalar character naming the response column in train
(and test if not NULL)
\item \code{true_params}: A named numeric vector where names exactly match
\code{vars_of_interest}
\item \code{vars_of_interest}: A non-empty character vector of unique names
\item \code{references}: A named numeric vector where names exactly match
\code{vars_of_interest}
\item \code{meta}: Optional named list with scalar values only
}

Validation rules:
\itemize{
\item \code{train} must be a data.frame with nrow >= 1
\item \code{test} must be NULL or a data.frame
\item \code{response} must be a scalar character present in train (and test if not NULL)
\item \code{true_params} must be a named numeric vector
\item \code{vars_of_interest} must be a non-empty unique character vector
\item \code{setequal(names(true_params), vars_of_interest)} must be TRUE
\item \code{setequal(names(references), vars_of_interest)} must be TRUE
\item No duplicate names in any named vector/list
\item \code{meta} must be a named list with scalar values only (if present)
}
}
\section{Errors}{

Throws a \code{bayesim_data_error} condition if validation fails.
}

\examples{
# Valid data bundle
data_bundle <- list(
  train = data.frame(x = 1:10, y = rnorm(10)),
  test = data.frame(x = 11:15, y = rnorm(5)),
  response = "y",
  true_params = c(beta = 1.5, sigma = 0.5),
  vars_of_interest = c("beta", "sigma"),
  references = c(beta = 0, sigma = 1)
)
validate_data_bundle(data_bundle)
}
