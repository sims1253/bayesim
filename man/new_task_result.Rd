% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results.R
\name{new_task_result}
\alias{new_task_result}
\title{Create a new bayesim_task_result object}
\usage{
new_task_result(
  task_id = character(1),
  status = "success",
  metrics = NULL,
  diagnostics = NULL,
  timing = list(total = 0),
  error = NULL,
  warnings = character()
)
}
\arguments{
\item{task_id}{Character scalar identifying the task}

\item{status}{Character scalar: one of "success", "failed", or "skipped"}

\item{metrics}{Named list of computed metrics (NULL if task failed or skipped)}

\item{diagnostics}{Named list of diagnostic values (NULL if task failed)}

\item{timing}{List containing timing information, must include \code{total}}

\item{error}{NULL, or a list with \code{error_class} and \code{error_message} if failed}

\item{warnings}{Character vector of warning messages}
}
\value{
A validated \code{bayesim_task_result} object
}
\description{
Constructs a result object from a single simulation task execution.
}
\details{
The bayesim_task_result class captures the outcome of a single simulation task,
including its computed metrics, any diagnostics, timing information, and
errors or warnings.

Validation rules:
\itemize{
\item \code{status} must be one of "success", "failed", or "skipped"
\item If \code{status} is "success", \code{metrics} must not be NULL
\item If \code{status} is "failed", \code{error} must not be NULL
\item \code{timing$total} must be non-negative
}
}
\examples{
\dontrun{
# Successful task
result <- new_task_result(
  task_id = "task_001",
  status = "success",
  metrics = list(rmse = 0.05, bias = 0.01),
  diagnostics = list(n_eff = 500, rhat = 1.01),
  timing = list(total = 5.2)
)

# Failed task
result <- new_task_result(
  task_id = "task_002",
  status = "failed",
  error = list(error_class = "convergence_error", error_message = "R-hat > 1.1"),
  timing = list(total = 2.0)
)
}
}
\keyword{internal}
