% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators.R
\name{ifs_generator}
\alias{ifs_generator}
\title{Construct an inverse forward sampling (IFS) data generator}
\usage{
ifs_generator(
  prefit,
  predictor_generator = NULL,
  vars_of_interest = NULL,
  response = NULL,
  lower_bound = NULL,
  upper_bound = NULL,
  truncate = FALSE
)
}
\arguments{
\item{prefit}{A brmsfit with posterior draws to sample theta from (the
preconditioning fit).}

\item{predictor_generator}{Function \verb{(data_spec, task_ctx) -> data.frame}
producing predictor covariates. Must consume the ambient RNG state. If
\code{NULL}, \code{prefit$data} is reused.}

\item{vars_of_interest}{Character vector naming the parameters to report as
\code{true_params}. Defaults to population-level effects.}

\item{response}{Name of the response column; defaults to the LHS of
\code{prefit}'s formula.}

\item{lower_bound, upper_bound}{Optional numeric bounds for the response
domain. If both \code{NULL} (default), no bounds are applied. If set, the
out-of-bounds policy is governed by \code{truncate}:
\itemize{
\item \code{truncate = FALSE} (default): out-of-bounds draws set the response to
all \code{NA}, which fails downstream validation and drops the replicate.
NOTE: this is NOT a draw-level resample — it drops the replicate's draw
from the SBC rank distribution, which biases ranks when violations are
non-uniform across the posterior. Use only when the bounds are soft.
\item \code{truncate = TRUE}: clamp out-of-bounds response values to the nearest
bound. This also biases the rank distribution (toward the bounds) but
keeps the replicate.
Neither option implements the plan's deterministic draw-resampling; both
are documented honestly here. A future version may add
\code{on_violation = "resample"}.
}}

\item{truncate}{Logical; if \code{TRUE} and bounds are set, clamp out-of-bounds
response values instead of producing NAs. Default \code{FALSE}.}
}
\value{
A generator function \verb{(data_spec, task_ctx) -> data_bundle}.
}
\description{
Draws a parameter vector theta from a preconditioning fit (typically a
posterior fit on a pilot dataset), uses it as the data-generating truth, and
forward-simulates data \code{y ~ p(y | theta)} respecting multivariate response
dependencies. Each task uses a distinct draw, deterministically indexed by
\code{task_ctx$rep_idx}.
}
\details{
Unlike \code{\link[=prior_predictive_generator]{prior_predictive_generator()}} (which samples from the prior), IFS
samples from a preconditioning posterior, concentrating the truth draw in a
region of high posterior mass. This is the canonical SBC generator for
models with diffuse or improper priors.
}
