% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-bank.R
\name{build_model_bank}
\alias{build_model_bank}
\title{Build the model bank for a BrmsFitter}
\usage{
build_model_bank(
  fitter,
  fit_grid,
  data_generator,
  data_spec_template,
  result_path = NULL
)
}
\arguments{
\item{fitter}{A BrmsFitter S7 object.}

\item{fit_grid}{A data.frame of model fitting specifications. Each row's
\code{formula}, \code{family}, \code{prior}, \code{stanvars} columns (list-columns) define a
model spec.}

\item{data_generator}{The simulation data generator function.}

\item{data_spec_template}{A named list (one row of \code{data_grid} as a list)
used to generate template data.}

\item{result_path}{NULL or character path. When non-NULL,
\code{options(cmdstanr_write_stan_file_dir)} is set to
\code{file.path(result_path, "stan_binaries")} so compiled binaries persist and
are shared across controller and local daemons.}
}
\value{
A named list of \code{brmsfit} prefit objects keyed by
\code{\link[=model_spec_hash]{model_spec_hash()}}, or NULL when \code{precompile} is FALSE.
}
\description{
For each DISTINCT row of \code{fit_grid} (deduped by \code{\link[=model_spec_hash]{model_spec_hash()}}), compiles
a prefit via \code{brms::brm(chains = 0)} against generator-supplied template
data. Returns a named list of prefit objects keyed by spec hash.
}
\details{
When \code{fitter@precompile} is FALSE, returns NULL immediately (the fitter falls
back to a fresh \code{brms::brm()} per task).

A compile failure is fatal: it raises a \code{\link[=bayesim_internal_error]{bayesim_internal_error()}} since the
simulation cannot proceed without a compilable model.
}
\keyword{internal}
