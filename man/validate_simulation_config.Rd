% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contracts.R
\name{validate_simulation_config}
\alias{validate_simulation_config}
\title{Validate Simulation Configuration}
\usage{
validate_simulation_config(config)
}
\arguments{
\item{config}{An S7 SimulationConfig object to validate.}
}
\value{
TRUE if validation passes.
}
\description{
Validates that a SimulationConfig object is complete and properly configured
for running a simulation. This includes validating the fitter interface,
all metrics, and the data_generator function signature.
}
\details{
The configuration is validated for:
\itemize{
\item Being a valid SimulationConfig S7 object
\item Having a non-NULL fitter that passes \code{\link[=validate_fitter_interface]{validate_fitter_interface()}}
\item Having metrics (if present) that pass \code{\link[=validate_metric_interface]{validate_metric_interface()}}
\item Having a data_generator function with the correct signature
}
}
\section{Errors}{

Throws a \code{bayesim_config_error} condition if validation fails.
}

\examples{
\dontrun{
config <- simulation_config(
  data_grid = data.frame(n = 100),
  fit_grid = data.frame(model = "baseline"),
  data_generator = my_data_gen,
  fitter = my_fitter,
  metrics = list(my_metric),
  n_replicates = 10L,
  seed = 42L
)
validate_simulation_config(config)
}
}
\seealso{
\code{\link[=simulation_config]{simulation_config()}}, \code{\link[=validate_fitter_interface]{validate_fitter_interface()}}, \code{\link[=validate_metric_interface]{validate_metric_interface()}}
}
\keyword{internal}
