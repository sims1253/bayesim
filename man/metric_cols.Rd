% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{metric_cols}
\alias{metric_cols}
\title{Select flattened metric columns}
\usage{
metric_cols(x, metric, fields = NULL, as = c("names", "long"))
}
\arguments{
\item{x}{A bayesim simulation result (with a \code{summary} component), or a
per-task summary data frame.}

\item{metric}{Required character scalar naming the metric prefix.}

\item{fields}{Optional character vector restricting the field segment.}

\item{as}{Output form: \code{"names"} returns a named character vector suitable
for \code{dplyr::all_of()}; \code{"long"} returns one row per task and metric value.}
}
\value{
For \code{as = "names"}, a named character vector whose values are full
column names and whose names are suffixes following the metric prefix. For
\code{as = "long"}, a tibble with optional \code{task_id}, \code{field}, \code{param}, and
\code{value} columns.
}
\description{
Selects columns belonging to one metric in bayesim's flattened naming
scheme, \verb{<metric>__<field>} or \verb{<metric>__<field>__<param>}.
}
\examples{
fitter <- LinearRegressionFitter()
summary <- data.frame(
  task_id = "task_1",
  posterior_summary__mean__x = 1.2,
  posterior_summary__sd__x = 0.3,
  check.names = FALSE
)
metric_cols(summary, "posterior_summary")
metric_cols(summary, "posterior_summary", fields = "mean", as = "long")
}
