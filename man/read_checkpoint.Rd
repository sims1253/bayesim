% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkpoint.R
\name{read_checkpoint}
\alias{read_checkpoint}
\title{Read checkpoint}
\usage{
read_checkpoint(result_path, checkpoint_id = NULL, load_outcomes = TRUE)
}
\arguments{
\item{result_path}{Character string giving the base path for results.
If NULL, the function returns NULL immediately.}

\item{checkpoint_id}{Integer specifying the checkpoint ID to read.
If NULL (default), reads the latest checkpoint from latest.json.}

\item{load_outcomes}{Logical; when FALSE, validate shard integrity but omit
materializing accumulated outcomes. Used by the filesystem RunStore while
appending the next ledger checkpoint.}
}
\value{
A list with the following elements, or NULL if checkpoint not found
or invalid:
\itemize{
\item \code{checkpoint_id} - Integer ID of the checkpoint
\item \code{meta} - List of checkpoint metadata
\item \code{task_grid} - Tibble/data.frame of task grid with status
\item \code{results_df} - Data frame of task results
}
}
\description{
Reads a checkpoint by ID, or the latest valid checkpoint if ID is not
specified. Verifies checksums before returning data.
}
\details{
The function performs the following steps:
\enumerate{
\item If checkpoint_id is NULL, read latest.json to get the latest ID
\item Construct checkpoint directory path from the ID
\item Verify directory exists
\item Verify checksums match
\item Read meta.json, ledger.rds, and results.rds
\item Return assembled checkpoint data
}

If the checkpoint has invalid checksums, a warning is issued and NULL
is returned. This allows the caller to fall back to earlier checkpoints.
}
\examples{
\dontrun{
# Read latest checkpoint
checkpoint <- read_checkpoint("/path/to/results")

# Read specific checkpoint
checkpoint <- read_checkpoint("/path/to/results", checkpoint_id = 5)
}
}
\seealso{
\code{\link[=write_checkpoint]{write_checkpoint()}}, \code{\link[=list_checkpoints]{list_checkpoints()}}, \code{\link[=validate_checkpoint_fingerprint]{validate_checkpoint_fingerprint()}}
}
\keyword{internal}
