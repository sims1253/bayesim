% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators.R
\name{prior_predictive_generator}
\alias{prior_predictive_generator}
\title{Construct a prior-predictive data generator}
\usage{
prior_predictive_generator(
  prior_fit,
  predictor_generator = NULL,
  vars_of_interest = NULL,
  response = NULL
)
}
\arguments{
\item{prior_fit}{A brmsfit compiled with \code{sample_prior = "only"} (or a
formula + family + prior combination to be compiled; see Details). Must
contain prior draws.}

\item{predictor_generator}{Function \verb{(data_spec, task_ctx) -> data.frame}
producing the design matrix of predictors (everything except the response).
Must consume the ambient RNG state. If \code{NULL}, the prior_fit's own data is
reused at its original size.}

\item{vars_of_interest}{Character vector naming the prior parameters to
report as \code{true_params} (defaults to all population-level effects
\code{"b_<name>"}, renamed to \verb{<name>}).}

\item{response}{Name of the response column (defaults to the LHS of
\code{prior_fit}'s formula).}
}
\value{
A generator function \verb{(data_spec, task_ctx) -> data_bundle}.
}
\description{
Draws a parameter vector theta from the model prior (via a \code{sample_prior = "only"} brmsfit) and simulates data \code{y ~ p(y | theta)} using
\code{\link[brms:posterior_predict]{brms::posterior_predict()}}. Each task uses a distinct prior draw,
deterministically indexed by \code{task_ctx$rep_idx}, so Simulation-Based
Calibration ranks are well-defined and resume is reproducible.
}
\details{
The prior model is compiled once (a \code{sample_prior = "only"} brmsfit); reuse
it across tasks via the model bank or by passing the same object. Predictor
covariates not implied by the prior are supplied by \code{predictor_generator}.
}
