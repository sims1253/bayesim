% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{n_replicates_for_target}
\alias{n_replicates_for_target}
\title{Required number of replicates for a target MCSE}
\usage{
n_replicates_for_target(
  target_mcse,
  metric_type = c("coverage", "continuous"),
  p = 0.5,
  assumed_sd = NULL
)
}
\arguments{
\item{target_mcse}{Numeric scalar > 0. The MCSE you want to achieve.}

\item{metric_type}{Character scalar: \code{"coverage"} (binary coverage metrics) or
\code{"continuous"} (bias / mean / model SE metrics).}

\item{p}{Numeric scalar in \verb{[0, 1]}. Assumed coverage probability. Only used
when \code{metric_type = "coverage"}. Defaults to \code{0.5}, the
variance-maximising (most conservative) choice.}

\item{assumed_sd}{Numeric scalar > 0. Assumed standard deviation of the
per-replicate point estimate. Required when
\code{metric_type = "continuous"}; ignored otherwise.}
}
\value{
An integer scalar: the number of replicates required (the ceiling of
the inverted-MCSE \code{n}).
}
\description{
Inverts the Monte-Carlo standard error (MCSE) formulas used by
\code{\link[=performance_measures]{performance_measures()}} to return the number of replicates \code{n} required to
achieve a target MCSE. Useful for planning a simulation study (a precision /
power calculation) before running it.

The MCSE formulas (Morris et al. 2019; rsimsum) invert to:
\itemize{
\item \strong{coverage} (binary 0/1 outcome):
\code{MCSE = sqrt(p (1 - p) / n)}  =>  \code{n = p (1 - p) / MCSE^2}.
The default \code{p = 0.5} is the conservative max-variance case (it
maximises \code{p (1 - p)} and therefore gives the largest required \code{n}).
\item \strong{continuous} metrics (bias / mean / model SE):
\code{MCSE = sd / sqrt(n)}  =>  \code{n = (sd / MCSE)^2}.
Requires an assumed standard deviation \code{assumed_sd} for the per-replicate
point estimate (e.g. a guess at the empirical SE of the estimator).
}

The returned value is \code{ceiling(n)} so it is always a whole number of
replicates.
}
\examples{
\dontrun{
# Coverage: target MCSE 0.03 under the conservative p = 0.5.
n_replicates_for_target(0.03, "coverage")

# Coverage at an assumed rate of 0.9.
n_replicates_for_target(0.03, "coverage", p = 0.9)

# Continuous metric (e.g. bias) with assumed sd of the point estimate.
n_replicates_for_target(0.05, "continuous", assumed_sd = 0.5)
}
}
\seealso{
\code{\link[=performance_measures]{performance_measures()}} for the MCSE formulas being inverted.
}
