% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{summarize_simulation}
\alias{summarize_simulation}
\title{Aggregate simulation results per condition}
\usage{
summarize_simulation(result, by = NULL, metrics = NULL)
}
\arguments{
\item{result}{A \code{bayesim_simulation_result} object (uses \code{result$summary}),
or a data.frame/tibble of per-task metrics. Passing the full result is
preferred: it carries the metrics' \code{summary_type} declarations.}

\item{by}{Character vector of grouping columns (conditions). Defaults to the
\verb{data_*}/\verb{fit_*} grid columns found in the summary plus any other
non-numeric condition columns (excluding \code{task_id} and \code{status}).}

\item{metrics}{Character vector of metric columns to aggregate. Defaults to
all numeric columns not in \code{by} and not metadata
(\code{task_id}, \code{rep_idx}, \code{status}, \verb{*timing*}).}
}
\value{
A tibble with one row per condition: the \code{by} columns, then for each
metric \verb{<m>_n_used}, \verb{<m>_mean}, \verb{<m>_median}, \verb{<m>_sd}, \verb{<m>_mcse}, plus
\code{n_reps}, \code{n_failed}, \code{failure_rate}. \verb{<m>_n_used} is the number of finite
values used for that metric; failed or non-finite metric values do not
contribute to its aggregate.
}
\description{
Computes per-condition aggregates of the wide summary tibble:
mean and median of each metric column, Monte Carlo standard errors
(MCSE), replicate counts, and failure/convergence-failure rates. Returns a
tidy tibble with one row per condition.

Aggregation follows each metric's declared \code{summary_type} (E4; see
\link{Metric}): \code{"mean"} columns get a \code{sd / sqrt(n)} MCSE, \code{"proportion"}
columns (e.g. coverage) get \code{sqrt(p(1-p) / n)}, and \code{"none"} columns
(e.g. SBC ranks) are excluded from aggregation. Columns from unknown or
user-defined sources default to \code{"mean"}. MCSE formulas follow rsimsum
(Gasparini, 2018).

Wide summaries: several metrics legitimately flatten to dozens of columns
each, so the default aggregation can return 100+ columns. Nothing is ever
dropped or truncated. Narrow the output with the \code{metrics} argument, and
discover a single metric's flattened columns with \code{\link[=metric_cols]{metric_cols()}}. In
interactive sessions only, a wide default call prints a one-line hint
pointing at these; programmatic and noninteractive use is always silent.
}
\examples{
\dontrun{
result <- run_simulation(config, progress = FALSE)
summarize_simulation(result)
summarize_simulation(result, by = "model", metrics = "rmse__value")
}
}
