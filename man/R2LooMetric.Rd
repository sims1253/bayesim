% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics-extended.R
\name{R2LooMetric}
\alias{R2LooMetric}
\alias{r2_loo_metric}
\title{R-squared LOO Metric}
\usage{
R2LooMetric(
  name = "r2_loo",
  needs = "loo",
  required = FALSE,
  summary_type = "mean",
  schema = list(value = list(role = "estimate", aggregation = "mean", mcse = "sd"), elpd
    = list(role = "estimate", aggregation = "mean", mcse = "sd"))
)

r2_loo_metric(name = "r2_loo")
}
\arguments{
\item{name}{Character string naming the metric. Defaults to "r2_loo".}
}
\value{
An \code{R2LooMetric} object.

An \code{R2LooMetric} object.
}
\description{
Variance-explained estimated via PSIS-LOO following brms'
\code{loo_R2()}: \code{1 - var_loo(y - yloo) / var_loo(y)}, where \code{yloo} is the
LOO-weighted posterior expectation (\code{\link[loo:E_loo]{loo::E_loo()}} mean of \code{posterior_epred})
and the variances use the same weighted-expecation construction as brms
(Gelman, Goodrich, Gabry & Vehtari 2018). Falls back to NA when epred or
the PSIS object is unavailable.

Constructor for R2LooMetric.
}
\examples{
r2_loo_metric()
}
\keyword{internal}
