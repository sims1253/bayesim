% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linear-regression-fitter.R
\name{LinearRegressionFitter}
\alias{LinearRegressionFitter}
\title{Conjugate Bayesian Linear Regression Fitter}
\usage{
LinearRegressionFitter(
  name = "linear_regression",
  supports_predictions = TRUE,
  supports_log_lik = TRUE,
  supports_loo = TRUE,
  n_draws = 1000L,
  prior_mean = 0,
  prior_precision = 1e-06,
  a0 = 2,
  b0 = 1e-06
)
}
\arguments{
\item{name}{Character string identifying the fitter.}

\item{supports_predictions}{Logical; whether predictions are supported.}

\item{supports_log_lik}{Logical; whether log-likelihood is supported.}

\item{supports_loo}{Logical; whether LOO-CV is supported.}

\item{n_draws}{Positive integer; number of i.i.d. posterior draws.}

\item{prior_mean}{Numeric vector (length = number of coefficients, including
intercept) or scalar; prior mean of \code{beta}. Recycled. Default 0.}

\item{prior_precision}{Numeric scalar; prior precision of \code{beta} per unit of
\code{sigma^2} (i.e. \code{Lambda0 = prior_precision * I}). A small value gives a
weak prior. Default \code{1e-6}.}

\item{a0}{Positive numeric; Inv-Gamma prior shape for \code{sigma^2}. Default \code{2}.}

\item{b0}{Positive numeric; Inv-Gamma prior rate for \code{sigma^2}. Default \code{1e-6}.}
}
\value{
An S7 \code{LinearRegressionFitter} object.
}
\description{
Exact conjugate Normal-Inverse-Gamma (NIG) Bayesian linear regression. Fits
\verb{y ~ N(X beta, sigma^2)} analytically and draws i.i.d. samples from the
joint posterior \verb{(beta, sigma)}. No Stan, milliseconds per fit, \strong{real
posteriors} — the package's teaching backbone (D1).

The model formula is taken from \code{fit_spec$formula} (a base R formula,
default \code{response ~ .}). Posterior draws use plain parameter names
(\code{Intercept}, \verb{<coef>}, \code{sigma}) so they line up with
\code{\link[=resolve_draw_columns]{resolve_draw_columns()}} and the generators' cleaned names out of the box.
}
\examples{
\dontrun{
fitter <- LinearRegressionFitter(n_draws = 500L)
}
}
\seealso{
\link{Fitter}, \link{BrmsFitter}, \code{\link[=CmdStanFitter]{CmdStanFitter()}}
}
