% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-fitter.R
\name{extract_brms_timings}
\alias{extract_brms_timings}
\title{Extract real warmup/sample/total timings from a brmsfit}
\usage{
extract_brms_timings(fit, fallback_total)
}
\arguments{
\item{fit}{A brmsfit object.}

\item{fallback_total}{Numeric length-1; used for \code{total} when timing can't be
extracted (e.g. the wall-clock elapsed time from a timer).}
}
\value{
Named list: \code{list(total, warmup, sample)}, all numeric scalars.
}
\description{
brms stores fit output as a \code{stanfit} object regardless of backend (cmdstanr
output is converted), so \code{\link[rstan:get_elapsed_time]{rstan::get_elapsed_time()}} works for both backends.
Returns per-chain CPU seconds summed across chains. Falls back to
\code{NA_real_} for warmup/sample (and the provided fallback total) when the
stanfit timing is unavailable (e.g. a chains = 0 prefit, or an interrupted run).
}
\keyword{internal}
