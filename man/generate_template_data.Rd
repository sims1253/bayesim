% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-bank.R
\name{generate_template_data}
\alias{generate_template_data}
\title{Generate template data for prefit compilation}
\usage{
generate_template_data(data_generator, data_spec, seed = 0L)
}
\arguments{
\item{data_generator}{The simulation data generator function.}

\item{data_spec}{A named list (one row of \code{data_grid} as a list).}

\item{seed}{Deprecated internal argument retained for compatibility; ignored.}
}
\value{
A data.frame suitable for \code{brms::brm(data =)}.
}
\description{
Produces ONE small dataset using the configured data generator with a
throwaway RNG stream. The dataset is discarded after compilation; its only
purpose is to fix the Stan data structure (variable names and types) so the
compiled binary matches the real task data.
}
\details{
Calls \code{data_generator(data_spec, task_ctx)} and returns its \verb{$train} data
frame. \code{task_ctx$template} is TRUE so generators can recognize this
structural template call.
}
\keyword{internal}
