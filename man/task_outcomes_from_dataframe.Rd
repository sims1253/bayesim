% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkpoint.R
\name{task_outcomes_from_dataframe}
\alias{task_outcomes_from_dataframe}
\title{Migrate the legacy flat checkpoint view to canonical task outcomes.}
\usage{
task_outcomes_from_dataframe(results_df)
}
\description{
The flat view produced by \code{\link[=results_to_dataframe]{results_to_dataframe()}} lays columns out in a
fixed order: \code{task_id}, \code{status}, \code{stop_reason}, metric columns,
\verb{truth__*} columns, diagnostic columns, then the optional
\code{error_class}/\code{error_message}/\code{timing_total} trailer. Metric and diagnostic
columns share a namespace, so the two groups are separated positionally
around the \code{truth__} block. When a row carries no truth block the groups are
contiguous and indistinguishable; every value column is then restored as a
metric so the flat representation still round-trips unchanged.
}
\keyword{internal}
