% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resume.R
\name{load_for_resume}
\alias{load_for_resume}
\title{Load Run for Resume}
\usage{
load_for_resume(result_path, config, run_store = NULL)
}
\arguments{
\item{result_path}{Character; path to results directory containing checkpoints.}

\item{config}{SimulationConfig; current configuration object.}

\item{run_store}{Optional internal \code{\link[=new_run_store]{new_run_store()}} adapter. When supplied,
it owns checkpoint reads and corruption fallback.}
}
\value{
A list with elements:
\itemize{
\item \code{task_grid}: Task grid with restored status from checkpoint
\item \code{prior_results}: Data frame of results from checkpoint
\item \code{checkpoint_id}: ID of the checkpoint being resumed from
}
}
\description{
Loads previous run state for resumption. Validates schema compatibility
and configuration fingerprint before allowing resume.
}
\details{
The function performs the following validation steps:
\enumerate{
\item Reads and validates run_manifest.json
\item Checks schema version compatibility
\item Computes and compares configuration fingerprints
\item Finds the most recent valid checkpoint
\item Rebuilds task grid with status from checkpoint
}
}
\examples{
\dontrun{
config <- simulation_config(...)
resume_state <- load_for_resume("/path/to/results", config)
task_grid <- resume_state$task_grid
prior_results <- resume_state$prior_results
}
}
\keyword{internal}
