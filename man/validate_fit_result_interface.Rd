% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contracts.R
\name{validate_fit_result_interface}
\alias{validate_fit_result_interface}
\title{Validate Fit Result Interface}
\usage{
validate_fit_result_interface(fit_result)
}
\arguments{
\item{fit_result}{A bayesim_fit_result object to validate.}
}
\value{
The input \code{fit_result}, invisibly, if validation passes.
}
\description{
Validates that a fit_result conforms to the bayesim_fit_result interface.
This is a wrapper around \code{\link[=validate_bayesim_fit_result]{validate_bayesim_fit_result()}} that provides
clear error messages for contract violations.
}
\details{
The fit_result must satisfy the bayesim_fit_result class requirements:
\itemize{
\item Must inherit from "bayesim_fit_result" class
\item If \code{success} is TRUE, \code{error} must be NULL
\item If \code{success} is FALSE, \code{error} must be non-NULL
\item \code{timing$total} must be a non-negative scalar numeric
\item If \code{draws} is not NULL, it must be a matrix with column names
\item \code{warnings} must be a character vector
\item \code{diagnostics} must be a list
}
}
\section{Errors}{

Throws a \code{bayesim_contract_error} condition if validation fails.
}

\examples{
\dontrun{
# Create a valid fit result
draws <- matrix(rnorm(100), ncol = 2, nrow = 50)
colnames(draws) <- c("alpha", "beta")
result <- new_fit_result(
  success = TRUE,
  draws = draws,
  diagnostics = list(rhat_max = 1.01),
  timing = list(total = 5.0, warmup = 2.5, sample = 2.5)
)
validate_fit_result_interface(result)
}
}
\seealso{
\code{\link[=validate_bayesim_fit_result]{validate_bayesim_fit_result()}}, \code{\link[=new_fit_result]{new_fit_result()}}
}
\keyword{internal}
