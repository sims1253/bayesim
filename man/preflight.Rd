% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runtime-ux.R
\name{preflight}
\alias{preflight}
\title{Preflight check for a simulation configuration}
\usage{
preflight(config, pilot = FALSE, condensed = FALSE)
}
\arguments{
\item{config}{A \code{SimulationConfig}.}

\item{pilot}{Logical; if TRUE, time a single pilot task to estimate total
wall-clock time (default FALSE). Experimental.}

\item{condensed}{Logical; if TRUE, print a single one-line summary instead of
the full report (used by run_simulation).}
}
\value{
Invisibly, a named list of preflight information.
}
\description{
Inspects a configuration \emph{before} a long run and reports: total task count,
grid dimensions, metrics and their \code{needs} vs the fitter's capabilities
(surfacing mismatch warnings before a 10-hour run), whether mirai daemons
are set, and (for BrmsFitter) the number of distinct models to compile.

When run automatically at the top of \code{\link[=run_simulation]{run_simulation()}}, it prints a
condensed one-line summary.
}
\examples{
\dontrun{
preflight(config)
}
}
\seealso{
\code{\link[=run_simulation]{run_simulation()}}, \code{\link[=simulation_config]{simulation_config()}}
}
