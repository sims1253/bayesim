% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/worker.R
\name{build_loo_context}
\alias{build_loo_context}
\title{Build the LOO context (elpd summary + PSIS object + epred)}
\usage{
build_loo_context(fitter, fit_result)
}
\value{
A list with elements \code{loo}, \code{psis}, \code{log_lik}, \code{epred}, or NULL on
failure. \code{psis}/\code{log_lik}/\code{epred} may be individually NULL if unavailable.
}
\description{
Constructs the full LOO context for F3's rmse_loo / r2_loo metrics. Computes
the elpd/p_loo/pareto_k summary (as the legacy \code{loo_fit()} did), the PSIS
object (for \code{loo::E_loo()} weighted predictions), the pointwise log-likelihood
matrix, and the posterior expectation predictions (epred) — all once, shared
across metrics.
}
\details{
The PSIS object uses \code{loo::psis(-ll, r_eff)} with per-observation
relative-efficiency factors derived from the fit's chain structure via
\code{posterior::as_draws_df(fit)$.chain} (matches brms' internal \code{r_eff_log_lik}
exactly). Falls back to \code{r_eff = NULL} (with a captured warning) when chain
structure is unavailable, which is mathematically valid but slightly less
accurate.

epred must be the posterior expectation (mu, no observation noise); for brms
this is \code{brms::posterior_epred}. The Fitter must expose this via
\code{predict_epred()}; fitters that cannot return NULL (the metrics then NA).
}
\keyword{internal}
