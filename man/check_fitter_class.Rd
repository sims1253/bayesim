% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contracts.R
\name{check_fitter_class}
\alias{check_fitter_class}
\alias{validate_fitter_interface}
\title{Validate Fitter Class Hierarchy}
\usage{
check_fitter_class(fitter)

validate_fitter_interface(fitter)
}
\arguments{
\item{fitter}{An S7 object to validate as a Fitter.}
}
\value{
The input \code{fitter}, invisibly, if validation passes.
}
\description{
Validates that a fitter object is an S7 instance of the Fitter class.
This is a lightweight class-hierarchy check used internally by
\code{\link[=validate_simulation_config]{validate_simulation_config()}}. For full interface validation including
method existence and optional smoke testing, use \code{\link[=validate_fitter]{validate_fitter()}}.
}
\details{
The fitter must satisfy the following requirements:
\itemize{
\item Must be an S7 object (checked via S7::S7_inherits())
\item Must inherit from the "Fitter" class
}

Method existence is not checked here because S7 methods are dispatched
via generics, not stored as properties. The Fitter base class uses
S7::stop_method_not_implemented() for abstract methods, so subclasses
that don't override will raise errors when methods are called.
}
\section{Errors}{

Throws a \code{bayesim_contract_error} condition if validation fails.
}

\examples{
\dontrun{
# Validate the mock fitter
mock_fitter <- MockFitter()
validate_fitter_interface(mock_fitter)
}
}
\seealso{
\link{Fitter}, \code{\link[=validate_fitter]{validate_fitter()}}
}
\keyword{internal}
